# Base URL for help-media. Each <HelpMedia id="slug" /> resolves to
# `${PUBLIC_HELP_MEDIA_BASE_URL}/{slug}.png` (or .mp4 for video).
# Override for staging builds; leave unset locally to use the production
# default (https://help-media.stemcounts.com).
PUBLIC_HELP_MEDIA_BASE_URL=
